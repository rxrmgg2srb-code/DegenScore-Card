# Database
DATABASE_URL="postgresql://user:password@host:port/database?pgbouncer=true"

# Helius API (get from: https://www.helius.dev/)
HELIUS_API_KEY="your-helius-api-key-here"
HELIUS_RPC_URL="https://mainnet.helius-rpc.com/?api-key=your-helius-api-key-here"

# Treasury Wallet (your Solana wallet for receiving payments)
TREASURY_WALLET="your-treasury-wallet-public-key"
NEXT_PUBLIC_TREASURY_WALLET="your-treasury-wallet-public-key"

# Payment Configuration
MINT_PRICE_SOL="0.2"  # Price for premium cards in SOL (default: 0.2 SOL ~$40)

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_SOLANA_NETWORK="devnet"  # or "mainnet-beta"
NEXT_PUBLIC_HELIUS_RPC_URL="https://api.devnet.solana.com"

# Cron Job API Key (for scheduled tasks)
CRON_API_KEY="your-cron-api-key-here"

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET="your-jwt-secret-minimum-32-characters"

# Admin Configuration (comma-separated wallet addresses)
ADMIN_WALLETS="admin-wallet-1,admin-wallet-2"

# Discord Webhook (optional, for community notifications)
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/your-webhook-url"
WEBHOOK_SECRET="your-webhook-secret-key-here"  # Genera con: openssl rand -base64 32

# Telegram Bot (optional, for notifications)
TELEGRAM_BOT_TOKEN="your-telegram-bot-token"
TELEGRAM_CHANNEL_ID="your-telegram-channel-id"

# Sentry Error Tracking (get from: https://sentry.io - FREE 5k errors/month)
NEXT_PUBLIC_SENTRY_DSN="https://your-key@o123456.ingest.sentry.io/123456"
SENTRY_ORG="your-sentry-org"
SENTRY_PROJECT="your-sentry-project"
SENTRY_AUTH_TOKEN="your-sentry-auth-token"  # Solo para builds, opcional

# Upstash Redis (get from: https://upstash.com - FREE 10k commands/day)
UPSTASH_REDIS_REST_URL="https://your-endpoint.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-upstash-token"

# ❌ Cloudflare R2 Storage - DISABLED
# Images are now stored as base64 data URIs in the database
# R2_ACCOUNT_ID="your-cloudflare-account-id"
# R2_ACCESS_KEY_ID="your-r2-access-key"
# R2_SECRET_ACCESS_KEY="your-r2-secret-key"
# R2_BUCKET_NAME="degenscore-images"
# R2_PUBLIC_URL="https://your-r2-bucket.r2.dev"

# Pusher Real-time (get from: https://pusher.com - FREE 200k messages/day)
PUSHER_APP_ID="your-pusher-app-id"
PUSHER_KEY="your-pusher-key"
PUSHER_SECRET="your-pusher-secret"
PUSHER_CLUSTER="us2"  # o el cluster que elijas
NEXT_PUBLIC_PUSHER_KEY="your-pusher-key"
NEXT_PUBLIC_PUSHER_CLUSTER="us2"
